server:
  port: 8080

spring:
  application:
    name: OrderServiceMS
  cloud:
    consul:
      discovery:
        hostname: localhost
      host: localhost
      config:
        fail-fast: false
        enabled: true
        prefix: config
        defaultContext: application
        data-key: data
        profileSeparator: '::'
        format: YAML